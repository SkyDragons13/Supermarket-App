<Page x:Class="Supermarket.View.ValoareCategoriiView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Supermarket.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ValoareCategoriiView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <!-- This row will take the remaining space -->
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="3" VerticalAlignment="Stretch" ItemsSource="{Binding CategoriiValoarePairs}" AutoGenerateColumns="False"
        ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <!-- Define only the columns you want to display -->
            <DataGrid.Columns>
                <!-- Display category names -->
                <DataGridTextColumn Header="Categorie" Binding="{Binding Categorie.Categorie_nume}" />

                <!-- Display category values -->
                <DataGridTextColumn Header="Valoare" Binding="{Binding Valoare}" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Page>
